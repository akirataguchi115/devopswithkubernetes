apiVersion: v1 # Includes the Service for lazyness
kind: Service
metadata:
  name: postgres-svc
  labels:
    app: postgres
spec:
  ports:
  - port: 6379
    name: web
  clusterIP: None
  selector:
    app: pingpongapp