apiVersion: v1
kind: Service
metadata:
  name: pingpongapp-svc
spec:
  type: ClusterIP
  selector:
    app: pingpongapp
  ports:
    - name: http
      protocol: TCP
      port: 2346
      targetPort: 3002